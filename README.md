# CRM Lead Management System (Minimal Working Backend)

This is a minimal Spring Boot project that **builds and runs successfully**.

## How to run

```bash
mvn spring-boot:run
```

Then open:

- http://localhost:8080/api/health
